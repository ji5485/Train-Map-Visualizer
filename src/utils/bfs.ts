import { TrainPlatformMatrixType, TrainLineMatrixType } from 'types/Train.types'

export default function findTrainLineWithBFS(
  trainPlatformMatrix: TrainPlatformMatrixType,
  trainLineMatrix: TrainLineMatrixType,
  row: number,
  column: number,
) {
  const queue = []
}
